# 운영 안전 원칙

이 프로젝트는 실제 베타 사용자가 데이터를 입력하는 운영 단계입니다.
모든 작업 전에 아래 원칙을 반드시 적용하세요.

## 필수 준수 규칙

1. 기존 Supabase 운영 DB의 데이터는 절대 삭제·초기화·리셋하지 않는다.
2. DROP TABLE, TRUNCATE, 전체 DELETE, 기존 데이터 reset 금지.
3. 기존 컬럼 삭제 금지.
4. 기존 기능 삭제 금지.
5. 기존 사용자 데이터가 들어 있는 테이블에 destructive migration 금지.
6. 필요한 DB 변경은 ADD COLUMN, CREATE TABLE, CREATE INDEX처럼 additive migration 방식으로만 진행한다.
7. 삭제 기능은 기본적으로 soft delete를 우선 사용한다.
8. 샘플 데이터 삭제는 반드시 isSample=true 데이터만 대상으로 한다.
9. 실제 고객 데이터와 샘플 데이터가 섞여 삭제되지 않게 한다.
10. SQL migration이 필요한 경우, 실제 Supabase SQL Editor에 적용하기 전에 반드시 전체 SQL을 먼저 출력하고 사용자 확인을 받는다.
11. 배포 전 기존 사용자 데이터가 보존되는지 확인한다.
12. 작업 전후 npm run build를 실행하고, 기존 핵심 흐름이 깨지지 않았는지 확인한다.
13. 운영 DB와 연결된 상태에서 테스트용 초기화/seed/reset 코드를 실행하지 않는다.
14. 기존 사용자가 입력한 업체, 직원, 서류, 수수료, 업무일지, 활동로그 데이터는 절대 임의로 삭제하지 않는다.
15. 변경 작업은 기존 기능을 유지한 상태에서 추가·수정 방식으로만 진행한다.
